spec:
  name: animeccha
  region: nyc
  static_sites:
    - build_command: yarn build
      catchall_document: index.html
      environment_slug: node-js
      github:
        branch: main
        deploy_on_push: true
        repo: lapstjup/animeccha
      name: animeccha
      output_dir: build
      routes:
        - path: /
